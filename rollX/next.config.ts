import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   // Add this line to enable the standalone output feature.
  output: 'standalone',
};

export default nextConfig;
